// import AbstractPlane from "./AbstractPlane";
// import { PlaneSlot } from "../interfaces";
// import * as d3 from 'd3'

// class Otter extends AbstractPlane {
//     // private w = 1.5
//     //  l = 6 
    
//     // protected computeSlots(): PlaneSlot[] {
        
//     //     const floaters = d3.range(7).map(y => ({x: -2, y: l - 0.5 - y}))
//     //     const inDoor = d3.range(3).map(y => ({ x: -1, y: l - 3 -y}))
//     //     const inDoor2 = d3.range(2).map(y => ({x: 0, y: l - 3.5 - y}))
//     //     const divers = d3.range(5).flatMap(y => [{ x: -0.5, y: -0.5 - y},
//     //                                        { x: 0.5, y: -0.5 - y}])
//     //     return [...floaters, ...inDoor, ...inDoor2, ...divers]
//     // }
    
// }

export default 0